<UserControl x:Class="DrugCompare.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border Background="White"
            CornerRadius="12"
            Padding="14"
            BorderBrush="#D6DEE8"
            BorderThickness="1"
            Margin="0,12,0,0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"
                  Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Interaction check history"
                               FontSize="18"
                               FontWeight="Bold"/>

                    <TextBlock Text="Recent locally saved interaction checks."
                               FontSize="12"
                               Foreground="#607080"
                               Margin="0,3,0,0"/>
                </StackPanel>

                <Button Grid.Column="1"
                        Content="Refresh history"
                        Command="{Binding LoadHistoryCommand}"
                        Width="140"
                        Height="34"/>
            </Grid>

            <DataGrid Grid.Row="1"
                      ItemsSource="{Binding InteractionHistory}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Date"
                                        Binding="{Binding CreatedAt, StringFormat={}{0:yyyy-MM-dd HH:mm}}"
                                        Width="150"/>

                    <DataGridTextColumn Header="Highest severity"
                                        Binding="{Binding HighestSeverity}"
                                        Width="130"/>

                    <DataGridTextColumn Header="Accepted substances"
                                        Binding="{Binding AcceptedSubstancesText}"
                                        Width="350"/>

                    <DataGridTextColumn Header="Results"
                                        Binding="{Binding ResultsText}"
                                        Width="500"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Border>
</UserControl>